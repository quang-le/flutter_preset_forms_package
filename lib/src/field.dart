import 'package:preset_form_fields/src/utils/generate.dart';
import 'package:preset_form_fields/src/utils/helpers.dart';
import 'package:preset_form_fields/src/input_formatters.dart';
import 'package:preset_form_fields/src/validate.dart';
import 'package:flutter/material.dart';
import 'package:flutter/services.dart';

class Field extends TextFormField with Validate, TextHelpers {
  Field.textForm(
      {Key key,
      this.dateFormat = DateFormat.eur,
      FocusNode focusNode,
      TextEditingController controller,
      String initialValue,
      InputDecoration decoration = const InputDecoration(),
      TextInputAction textInputAction,
      TextStyle style,
      StrutStyle strutStyle,
      TextDirection textDirection,
      TextAlign textAlign = TextAlign.start,
      ToolbarOptions toolbarOptions,
      bool autofocus = false,
      bool readOnly = false,
      bool showCursor,
      bool obscureText = false,
      bool autocorrect = true,
      bool autovalidate = false,
      bool maxLengthEnforced = true,
      int maxLines = 1,
      int minLines,
      bool expands = false,
      int maxLength,
      ValueChanged<String> onChanged,
      GestureTapCallback onTap,
      VoidCallback onEditingComplete,
      ValueChanged<String> onFieldSubmitted,
      bool enabled = true,
      double cursorWidth = 2.0,
      Radius cursorRadius,
      Color cursorColor,
      Brightness keyboardAppearance,
      EdgeInsets scrollPadding = const EdgeInsets.all(20.0),
      bool enableInteractiveSelection = true,
      InputCounterWidgetBuilder buildCounter,
      GlobalKey formKey,
      FormFieldValidator<String> validator,
      List<TextInputFormatter> inputFormatters,
      FormFieldSetter<String> onSaved,
      TextInputType keyboardType})
      : assert(controller != null),
        super(
            key: formKey ?? key,
            focusNode: focusNode,
            controller: controller,
            initialValue: initialValue,
            decoration: decoration,
            textInputAction: textInputAction,
            style: style,
            strutStyle: strutStyle,
            textDirection: textDirection,
            textAlign: textAlign,
            autofocus: autofocus,
            readOnly: readOnly,
            toolbarOptions: toolbarOptions,
            showCursor: showCursor,
            obscureText: obscureText,
            autocorrect: autocorrect,
            autovalidate: autovalidate,
            maxLengthEnforced: maxLengthEnforced,
            maxLines: maxLines,
            minLines: minLines,
            expands: expands,
            maxLength: maxLength,
            onTap: onTap,
            onChanged: Generate.onChanged(controller, onChanged),
            onEditingComplete: onEditingComplete,
            onFieldSubmitted: onFieldSubmitted,
            enabled: enabled,
            cursorWidth: cursorWidth,
            cursorRadius: cursorRadius,
            cursorColor: cursorColor,
            keyboardAppearance: keyboardAppearance,
            scrollPadding: scrollPadding,
            enableInteractiveSelection: enableInteractiveSelection,
            buildCounter: buildCounter,
            validator: validator,
            inputFormatters: inputFormatters,
            onSaved: Generate.onSaved(controller, onSaved),
            keyboardType: keyboardType);

  Field.firstName({
    Key key,
    this.dateFormat = DateFormat.eur,
    FocusNode focusNode,
    String initialValue,
    TextEditingController controller,
    InputDecoration decoration = const InputDecoration(),
    TextInputAction textInputAction,
    TextStyle style,
    StrutStyle strutStyle,
    TextDirection textDirection,
    TextAlign textAlign = TextAlign.start,
    ToolbarOptions toolbarOptions,
    bool autofocus = false,
    bool readOnly = false,
    bool showCursor,
    bool obscureText = false,
    bool autocorrect = true,
    bool autovalidate = false,
    bool maxLengthEnforced = true,
    int maxLines = 1,
    int minLines,
    bool expands = false,
    int maxLength,
    ValueChanged<String> onChanged,
    GestureTapCallback onTap,
    VoidCallback onEditingComplete,
    ValueChanged<String> onFieldSubmitted,
    bool enabled = true,
    double cursorWidth = 2.0,
    Radius cursorRadius,
    Color cursorColor,
    Brightness keyboardAppearance,
    EdgeInsets scrollPadding = const EdgeInsets.all(20.0),
    bool enableInteractiveSelection = true,
    InputCounterWidgetBuilder buildCounter,
    GlobalKey formKey,
    FormFieldSetter<String> onSaved,
    TextInputType keyboardType,
    FormFieldValidator<String> validator = Validate.name,
  })  : assert(controller != null),
        super(
          key: formKey ?? key,
          focusNode: focusNode,
          controller: controller,
          initialValue: initialValue,
          decoration: decoration,
          textInputAction: textInputAction,
          style: style,
          strutStyle: strutStyle,
          textDirection: textDirection,
          textAlign: textAlign,
          autofocus: autofocus,
          readOnly: readOnly,
          toolbarOptions: toolbarOptions,
          showCursor: showCursor,
          obscureText: obscureText,
          autocorrect: autocorrect,
          autovalidate: autovalidate,
          maxLengthEnforced: maxLengthEnforced,
          maxLines: maxLines,
          minLines: minLines,
          expands: expands,
          maxLength: maxLength,
          onTap: onTap,
          onChanged: Generate.onChanged(controller, onChanged),
          onEditingComplete: onEditingComplete,
          onFieldSubmitted: onFieldSubmitted,
          enabled: enabled,
          cursorWidth: cursorWidth,
          cursorRadius: cursorRadius,
          cursorColor: cursorColor,
          keyboardAppearance: keyboardAppearance,
          scrollPadding: scrollPadding,
          enableInteractiveSelection: enableInteractiveSelection,
          buildCounter: buildCounter,
          validator: validator,
          inputFormatters: [
            BlacklistingTextInputFormatter(TextHelpers.namesRegExp()),
            NameInputFormatter(),
          ],
          onSaved: Generate.onSaved(controller, onSaved),
          keyboardType: keyboardType,
        );

  Field.lastName({
    Key key,
    this.dateFormat = DateFormat.eur,
    FocusNode focusNode,
    String initialValue,
    TextEditingController controller,
    InputDecoration decoration = const InputDecoration(),
    TextInputAction textInputAction,
    TextStyle style,
    StrutStyle strutStyle,
    TextDirection textDirection,
    TextAlign textAlign = TextAlign.start,
    ToolbarOptions toolbarOptions,
    bool autofocus = false,
    bool readOnly = false,
    bool showCursor,
    bool obscureText = false,
    bool autocorrect = true,
    bool autovalidate = false,
    bool maxLengthEnforced = true,
    int maxLines = 1,
    int minLines,
    bool expands = false,
    int maxLength,
    ValueChanged<String> onChanged,
    GestureTapCallback onTap,
    VoidCallback onEditingComplete,
    ValueChanged<String> onFieldSubmitted,
    bool enabled = true,
    double cursorWidth = 2.0,
    Radius cursorRadius,
    Color cursorColor,
    Brightness keyboardAppearance,
    EdgeInsets scrollPadding = const EdgeInsets.all(20.0),
    bool enableInteractiveSelection = true,
    InputCounterWidgetBuilder buildCounter,
    GlobalKey formKey,
    FormFieldSetter<String> onSaved,
    TextInputType keyboardType,
    FormFieldValidator<String> validator = Validate.name,
  })  : assert(controller != null),
        super(
          key: formKey ?? key,
          focusNode: focusNode,
          controller: controller,
          initialValue: initialValue,
          decoration: decoration,
          textInputAction: textInputAction,
          style: style,
          strutStyle: strutStyle,
          textDirection: textDirection,
          textAlign: textAlign,
          autofocus: autofocus,
          readOnly: readOnly,
          toolbarOptions: toolbarOptions,
          showCursor: showCursor,
          obscureText: obscureText,
          autocorrect: autocorrect,
          autovalidate: autovalidate,
          maxLengthEnforced: maxLengthEnforced,
          maxLines: maxLines,
          minLines: minLines,
          expands: expands,
          maxLength: maxLength,
          onTap: onTap,
          onChanged: Generate.onChanged(controller, onChanged),
          onEditingComplete: onEditingComplete,
          onFieldSubmitted: onFieldSubmitted,
          enabled: enabled,
          cursorWidth: cursorWidth,
          cursorRadius: cursorRadius,
          cursorColor: cursorColor,
          keyboardAppearance: keyboardAppearance,
          scrollPadding: scrollPadding,
          enableInteractiveSelection: enableInteractiveSelection,
          buildCounter: buildCounter,
          validator: validator,
          inputFormatters: [
            BlacklistingTextInputFormatter(TextHelpers.namesRegExp()),
            NameInputFormatter(keepLowerCase: TextHelpers.nameParticles()),
          ],
          onSaved: Generate.onSaved(controller, onSaved),
          keyboardType: keyboardType,
        );

  final DateFormat dateFormat;
  Field.email({
    Key key,
    this.dateFormat = DateFormat.eur,
    FocusNode focusNode,
    String initialValue,
    TextEditingController controller,
    InputDecoration decoration = const InputDecoration(),
    TextInputAction textInputAction,
    TextStyle style,
    StrutStyle strutStyle,
    TextDirection textDirection,
    TextAlign textAlign = TextAlign.start,
    ToolbarOptions toolbarOptions,
    bool autofocus = false,
    bool readOnly = false,
    bool showCursor,
    bool obscureText = false,
    bool autocorrect = true,
    bool autovalidate = false,
    bool maxLengthEnforced = true,
    int maxLines = 1,
    int minLines,
    bool expands = false,
    int maxLength,
    ValueChanged<String> onChanged,
    GestureTapCallback onTap,
    VoidCallback onEditingComplete,
    ValueChanged<String> onFieldSubmitted,
    bool enabled = true,
    double cursorWidth = 2.0,
    Radius cursorRadius,
    Color cursorColor,
    Brightness keyboardAppearance,
    EdgeInsets scrollPadding = const EdgeInsets.all(20.0),
    bool enableInteractiveSelection = true,
    InputCounterWidgetBuilder buildCounter,
    GlobalKey formKey,
    FormFieldSetter<String> onSaved,
    TextInputType keyboardType,
    FormFieldValidator<String> validator = Validate.email,
  })  : assert(controller != null),
        super(
          key: formKey ?? key,
          focusNode: focusNode,
          controller: controller,
          initialValue: initialValue,
          decoration: decoration,
          textInputAction: textInputAction,
          style: style,
          strutStyle: strutStyle,
          textDirection: textDirection,
          textAlign: textAlign,
          autofocus: autofocus,
          readOnly: readOnly,
          toolbarOptions: toolbarOptions,
          showCursor: showCursor,
          obscureText: obscureText,
          autocorrect: autocorrect,
          autovalidate: autovalidate,
          maxLengthEnforced: maxLengthEnforced,
          maxLines: maxLines,
          minLines: minLines,
          expands: expands,
          maxLength: maxLength,
          onChanged: Generate.onChanged(controller, onChanged),
          onTap: onTap,
          onEditingComplete: onEditingComplete,
          onFieldSubmitted: onFieldSubmitted,
          enabled: enabled,
          cursorWidth: cursorWidth,
          cursorRadius: cursorRadius,
          cursorColor: cursorColor,
          keyboardAppearance: keyboardAppearance,
          scrollPadding: scrollPadding,
          enableInteractiveSelection: enableInteractiveSelection,
          buildCounter: buildCounter,
          validator: validator,
          inputFormatters: [EmailInputFormatter()],
          onSaved: Generate.onSaved(controller, onSaved),
          keyboardType: keyboardType,
        );

  Field.date({
    Key key,
    this.dateFormat = DateFormat.eur,
    FocusNode focusNode,
    TextEditingController controller,
    String initialValue,
    InputDecoration decoration = const InputDecoration(),
    TextInputAction textInputAction,
    TextStyle style,
    StrutStyle strutStyle,
    TextDirection textDirection,
    TextAlign textAlign = TextAlign.start,
    ToolbarOptions toolbarOptions,
    bool autofocus = false,
    bool readOnly = false,
    bool showCursor,
    bool obscureText = false,
    bool autocorrect = true,
    bool autovalidate = false,
    bool maxLengthEnforced = true,
    int maxLines = 1,
    int minLines,
    bool expands = false,
    int maxLength,
    ValueChanged<String> onChanged,
    GestureTapCallback onTap,
    VoidCallback onEditingComplete,
    ValueChanged<String> onFieldSubmitted,
    bool enabled = true,
    double cursorWidth = 2.0,
    Radius cursorRadius,
    Color cursorColor,
    Brightness keyboardAppearance,
    EdgeInsets scrollPadding = const EdgeInsets.all(20.0),
    bool enableInteractiveSelection = true,
    InputCounterWidgetBuilder buildCounter,
    GlobalKey formKey,
    FormFieldSetter<String> onSaved,
    //TextInputType keyboardType,
    FormFieldValidator<String> validator,
  })  : assert(controller != null),
        super(
          key: formKey ?? key,
          focusNode: focusNode,
          controller: controller,
          initialValue: initialValue,
          decoration: decoration,
          textInputAction: textInputAction,
          style: style,
          strutStyle: strutStyle,
          textDirection: textDirection,
          textAlign: textAlign,
          autofocus: autofocus,
          readOnly: readOnly,
          toolbarOptions: toolbarOptions,
          showCursor: showCursor,
          obscureText: obscureText,
          autocorrect: autocorrect,
          autovalidate: autovalidate,
          maxLengthEnforced: maxLengthEnforced,
          maxLines: maxLines,
          minLines: minLines,
          expands: expands,
          maxLength: maxLength,
          onChanged: Generate.onChanged(controller, onChanged),
          onTap: onTap,
          onEditingComplete: onEditingComplete,
          onFieldSubmitted: onFieldSubmitted,
          enabled: enabled,
          cursorWidth: cursorWidth,
          cursorRadius: cursorRadius,
          cursorColor: cursorColor,
          keyboardAppearance: keyboardAppearance,
          scrollPadding: scrollPadding,
          enableInteractiveSelection: enableInteractiveSelection,
          buildCounter: buildCounter,
          validator: validator ?? Validate.date(dateFormat),
          inputFormatters: [DateInputFormatter()],
          // not using html sanitizer for this field as it would interfere with parsing
          // InputFormatter and keyboard type should prevent unwanted characters.
          onSaved: Generate.onSavedDate(controller, dateFormat, onSaved),
          keyboardType: TextInputType.phone,
        );

  // No default validator so it can be used for zip code and house/appt number.
  // inputFormatter tolerates letters so house numbers like 5B can be input.
  Field.number({
    Key key,
    this.dateFormat = DateFormat.eur,
    FocusNode focusNode,
    TextEditingController controller,
    String initialValue,
    InputDecoration decoration = const InputDecoration(),
    TextInputAction textInputAction,
    TextStyle style,
    StrutStyle strutStyle,
    TextDirection textDirection,
    TextAlign textAlign = TextAlign.start,
    ToolbarOptions toolbarOptions,
    bool autofocus = false,
    bool readOnly = false,
    bool showCursor,
    bool obscureText = false,
    bool autocorrect = true,
    bool autovalidate = false,
    bool maxLengthEnforced = true,
    int maxLines = 1,
    int minLines,
    bool expands = false,
    int maxLength,
    ValueChanged<String> onChanged,
    GestureTapCallback onTap,
    VoidCallback onEditingComplete,
    ValueChanged<String> onFieldSubmitted,
    bool enabled = true,
    double cursorWidth = 2.0,
    Radius cursorRadius,
    Color cursorColor,
    Brightness keyboardAppearance,
    EdgeInsets scrollPadding = const EdgeInsets.all(20.0),
    bool enableInteractiveSelection = true,
    InputCounterWidgetBuilder buildCounter,
    GlobalKey formKey,
    FormFieldSetter<String> onSaved,
    TextInputType keyboardType,
    FormFieldValidator<String> validator,
  })  : assert(controller != null),
        super(
          key: formKey ?? key,
          focusNode: focusNode,
          controller: controller,
          initialValue: initialValue,
          decoration: decoration,
          textInputAction: textInputAction,
          style: style,
          strutStyle: strutStyle,
          textDirection: textDirection,
          textAlign: textAlign,
          autofocus: autofocus,
          readOnly: readOnly,
          toolbarOptions: toolbarOptions,
          showCursor: showCursor,
          obscureText: obscureText,
          autocorrect: autocorrect,
          autovalidate: autovalidate,
          maxLengthEnforced: maxLengthEnforced,
          maxLines: maxLines,
          minLines: minLines,
          expands: expands,
          maxLength: maxLength,
          onChanged: Generate.onChanged(controller, onChanged),
          onTap: onTap,
          onEditingComplete: onEditingComplete,
          onFieldSubmitted: onFieldSubmitted,
          enabled: enabled,
          cursorWidth: cursorWidth,
          cursorRadius: cursorRadius,
          cursorColor: cursorColor,
          keyboardAppearance: keyboardAppearance,
          scrollPadding: scrollPadding,
          enableInteractiveSelection: enableInteractiveSelection,
          buildCounter: buildCounter,
          validator: validator,
          inputFormatters: [
            WhitelistingTextInputFormatter(TextHelpers.numbersAndLetters())
          ],
          onSaved: Generate.onSaved(controller, onSaved),
          keyboardType: keyboardType,
        );

  // differs from other constructors as it has inputFormatters value can be set by user.
  // text is being sanitized onSaved to avoid messing up with displayed text
  Field.address({
    Key key,
    this.dateFormat = DateFormat.eur,
    FocusNode focusNode,
    TextEditingController controller,
    String initialValue,
    InputDecoration decoration = const InputDecoration(),
    TextInputAction textInputAction,
    TextStyle style,
    StrutStyle strutStyle,
    TextDirection textDirection,
    TextAlign textAlign = TextAlign.start,
    ToolbarOptions toolbarOptions,
    bool autofocus = false,
    bool readOnly = false,
    bool showCursor,
    bool obscureText = false,
    bool autocorrect = true,
    bool autovalidate = false,
    bool maxLengthEnforced = true,
    int maxLines = 1,
    int minLines,
    bool expands = false,
    int maxLength,
    ValueChanged<String> onChanged,
    GestureTapCallback onTap,
    VoidCallback onEditingComplete,
    ValueChanged<String> onFieldSubmitted,
    bool enabled = true,
    double cursorWidth = 2.0,
    Radius cursorRadius,
    Color cursorColor,
    Brightness keyboardAppearance,
    EdgeInsets scrollPadding = const EdgeInsets.all(20.0),
    bool enableInteractiveSelection = true,
    InputCounterWidgetBuilder buildCounter,
    GlobalKey formKey,
    FormFieldSetter<String> onSaved,
    TextInputType keyboardType,
    FormFieldValidator<String> validator,
    List<TextInputFormatter> inputFormatters,
  })  : assert(controller != null),
        super(
          key: formKey ?? key,
          focusNode: focusNode,
          controller: controller,
          initialValue: initialValue,
          decoration: decoration,
          textInputAction: textInputAction,
          style: style,
          strutStyle: strutStyle,
          textDirection: textDirection,
          textAlign: textAlign,
          autofocus: autofocus,
          readOnly: readOnly,
          toolbarOptions: toolbarOptions,
          showCursor: showCursor,
          obscureText: obscureText,
          autocorrect: autocorrect,
          autovalidate: autovalidate,
          maxLengthEnforced: maxLengthEnforced,
          maxLines: maxLines,
          minLines: minLines,
          expands: expands,
          maxLength: maxLength,
          onChanged: Generate.onChanged(controller, onChanged),
          onTap: onTap,
          onEditingComplete: onEditingComplete,
          onFieldSubmitted: onFieldSubmitted,
          enabled: enabled,
          cursorWidth: cursorWidth,
          cursorRadius: cursorRadius,
          cursorColor: cursorColor,
          keyboardAppearance: keyboardAppearance,
          scrollPadding: scrollPadding,
          enableInteractiveSelection: enableInteractiveSelection,
          buildCounter: buildCounter,
          validator: validator,
          inputFormatters: inputFormatters,
          onSaved: Generate.onSaved(controller, onSaved),
          keyboardType: keyboardType,
        );

  Field.country({
    Key key,
    this.dateFormat = DateFormat.eur,
    FocusNode focusNode,
    TextEditingController controller,
    String initialValue,
    InputDecoration decoration = const InputDecoration(),
    TextInputAction textInputAction,
    TextStyle style,
    StrutStyle strutStyle,
    TextDirection textDirection,
    TextAlign textAlign = TextAlign.start,
    ToolbarOptions toolbarOptions,
    bool autofocus = false,
    bool readOnly = false,
    bool showCursor,
    bool obscureText = false,
    bool autocorrect = true,
    bool autovalidate = false,
    bool maxLengthEnforced = true,
    int maxLines = 1,
    int minLines,
    bool expands = false,
    int maxLength,
    ValueChanged<String> onChanged,
    GestureTapCallback onTap,
    VoidCallback onEditingComplete,
    ValueChanged<String> onFieldSubmitted,
    bool enabled = true,
    double cursorWidth = 2.0,
    Radius cursorRadius,
    Color cursorColor,
    Brightness keyboardAppearance,
    EdgeInsets scrollPadding = const EdgeInsets.all(20.0),
    bool enableInteractiveSelection = true,
    InputCounterWidgetBuilder buildCounter,
    GlobalKey formKey,
    FormFieldSetter<String> onSaved,
    TextInputType keyboardType,
    FormFieldValidator<String> validator,
  })  : assert(controller != null),
        super(
          key: formKey ?? key,
          focusNode: focusNode,
          controller: controller,
          initialValue: initialValue,
          decoration: decoration,
          textInputAction: textInputAction,
          style: style,
          strutStyle: strutStyle,
          textDirection: textDirection,
          textAlign: textAlign,
          autofocus: autofocus,
          readOnly: readOnly,
          toolbarOptions: toolbarOptions,
          showCursor: showCursor,
          obscureText: obscureText,
          autocorrect: autocorrect,
          autovalidate: autovalidate,
          maxLengthEnforced: maxLengthEnforced,
          maxLines: maxLines,
          minLines: minLines,
          expands: expands,
          maxLength: maxLength,
          onChanged: Generate.onChanged(controller, onChanged),
          onTap: onTap,
          onEditingComplete: onEditingComplete,
          onFieldSubmitted: onFieldSubmitted,
          enabled: enabled,
          cursorWidth: cursorWidth,
          cursorRadius: cursorRadius,
          cursorColor: cursorColor,
          keyboardAppearance: keyboardAppearance,
          scrollPadding: scrollPadding,
          enableInteractiveSelection: enableInteractiveSelection,
          buildCounter: buildCounter,
          validator: validator,
          inputFormatters: [
            CountryInputFormatter(),
            WhitelistingTextInputFormatter(TextHelpers.letters())
          ],
          onSaved: Generate.onSaved(controller, onSaved),
          keyboardType: keyboardType,
        );
  Field.phone({
    Key key,
    this.dateFormat = DateFormat.eur,
    FocusNode focusNode,
    TextEditingController controller,
    String initialValue,
    InputDecoration decoration = const InputDecoration(),
    TextInputAction textInputAction,
    TextStyle style,
    StrutStyle strutStyle,
    TextDirection textDirection,
    TextAlign textAlign = TextAlign.start,
    ToolbarOptions toolbarOptions,
    bool autofocus = false,
    bool readOnly = false,
    bool showCursor,
    bool obscureText = false,
    bool autocorrect = true,
    bool autovalidate = false,
    bool maxLengthEnforced = true,
    int maxLines = 1,
    int minLines,
    bool expands = false,
    int maxLength,
    ValueChanged<String> onChanged,
    GestureTapCallback onTap,
    VoidCallback onEditingComplete,
    ValueChanged<String> onFieldSubmitted,
    bool enabled = true,
    double cursorWidth = 2.0,
    Radius cursorRadius,
    Color cursorColor,
    Brightness keyboardAppearance,
    EdgeInsets scrollPadding = const EdgeInsets.all(20.0),
    bool enableInteractiveSelection = true,
    InputCounterWidgetBuilder buildCounter,
    GlobalKey formKey,
    FormFieldSetter<String> onSaved,
    //uncomment if you want to be able to set the keyboard type
    //TextInputType keyboardType,
    FormFieldValidator<String> validator,
  })  : assert(controller != null),
        super(
          key: formKey ?? key,
          focusNode: focusNode,
          controller: controller,
          initialValue: initialValue,
          decoration: decoration,
          textInputAction: textInputAction,
          style: style,
          strutStyle: strutStyle,
          textDirection: textDirection,
          textAlign: textAlign,
          autofocus: autofocus,
          readOnly: readOnly,
          toolbarOptions: toolbarOptions,
          showCursor: showCursor,
          obscureText: obscureText,
          autocorrect: autocorrect,
          autovalidate: autovalidate,
          maxLengthEnforced: maxLengthEnforced,
          maxLines: maxLines,
          minLines: minLines,
          expands: expands,
          maxLength: maxLength,
          onChanged: Generate.onChanged(controller, onChanged),
          onTap: onTap,
          onEditingComplete: onEditingComplete,
          onFieldSubmitted: onFieldSubmitted,
          enabled: enabled,
          cursorWidth: cursorWidth,
          cursorRadius: cursorRadius,
          cursorColor: cursorColor,
          keyboardAppearance: keyboardAppearance,
          scrollPadding: scrollPadding,
          enableInteractiveSelection: enableInteractiveSelection,
          buildCounter: buildCounter,
          validator: validator,
          inputFormatters: [
            WhitelistingTextInputFormatter(TextHelpers.numbers())
          ],
          onSaved: Generate.onSaved(controller, onSaved),
          keyboardType: TextInputType.phone,
        );

  //No input formatters. htmlChars are converted onSaved
  Field.companyName({
    Key key,
    FocusNode focusNode,
    this.dateFormat = DateFormat.eur,
    String initialValue,
    TextEditingController controller,
    InputDecoration decoration = const InputDecoration(),
    TextInputAction textInputAction,
    TextStyle style,
    StrutStyle strutStyle,
    TextDirection textDirection,
    TextAlign textAlign = TextAlign.start,
    ToolbarOptions toolbarOptions,
    bool autofocus = false,
    bool readOnly = false,
    bool showCursor,
    bool obscureText = false,
    bool autocorrect = true,
    bool autovalidate = false,
    bool maxLengthEnforced = true,
    int maxLines = 1,
    int minLines,
    bool expands = false,
    int maxLength,
    ValueChanged<String> onChanged,
    GestureTapCallback onTap,
    VoidCallback onEditingComplete,
    ValueChanged<String> onFieldSubmitted,
    bool enabled = true,
    double cursorWidth = 2.0,
    Radius cursorRadius,
    Color cursorColor,
    Brightness keyboardAppearance,
    EdgeInsets scrollPadding = const EdgeInsets.all(20.0),
    bool enableInteractiveSelection = true,
    InputCounterWidgetBuilder buildCounter,
    GlobalKey formKey,
    FormFieldSetter<String> onSaved,
    TextInputType keyboardType,
    FormFieldValidator<String> validator = Validate.name,
  })  : assert(controller != null),
        super(
          key: formKey ?? key,
          focusNode: focusNode,
          controller: controller,
          initialValue: initialValue,
          decoration: decoration,
          textInputAction: textInputAction,
          style: style,
          strutStyle: strutStyle,
          textDirection: textDirection,
          textAlign: textAlign,
          autofocus: autofocus,
          readOnly: readOnly,
          toolbarOptions: toolbarOptions,
          showCursor: showCursor,
          obscureText: obscureText,
          autocorrect: autocorrect,
          autovalidate: autovalidate,
          maxLengthEnforced: maxLengthEnforced,
          maxLines: maxLines,
          minLines: minLines,
          expands: expands,
          maxLength: maxLength,
          onTap: onTap,
          onChanged: Generate.onChanged(controller, onChanged),
          onEditingComplete: onEditingComplete,
          onFieldSubmitted: onFieldSubmitted,
          enabled: enabled,
          cursorWidth: cursorWidth,
          cursorRadius: cursorRadius,
          cursorColor: cursorColor,
          keyboardAppearance: keyboardAppearance,
          scrollPadding: scrollPadding,
          enableInteractiveSelection: enableInteractiveSelection,
          buildCounter: buildCounter,
          validator: validator,
          onSaved: Generate.onSaved(controller, onSaved),
          keyboardType: keyboardType,
        );
}

enum DateFormat { eur, us }
